#ifndef TEXT_BUFFER_H\n#define TEXT_BUFFER_H\n\n#include <stdlib.h>\n#include <stdio.h>\n#include <string.h>\n\ntypedef struct {\n    char **lines; // Dynamic array to hold the lines of text\n    size_t line_count; // Number of lines currently in the buffer\n    size_t capacity; // Capacity of the dynamic array\n    size_t cursor_row; // Current cursor row position\n    size_t cursor_col; // Current cursor column position\n} TextBuffer;\n\n// Function Prototypes\nTextBuffer* init_text_buffer();\nvoid free_text_buffer(TextBuffer *buffer);\nvoid append_line(TextBuffer *buffer, const char *line);\nvoid insert_character(TextBuffer *buffer, char c);\nvoid delete_character(TextBuffer *buffer);\nvoid move_cursor(TextBuffer *buffer, int row_change, int col_change);\n\n#endif // TEXT_BUFFER_H